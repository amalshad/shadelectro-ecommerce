<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Item Invoice - <%= item.product ? item.product.productName : 'Item' %> - ShadElectro</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: '#fd7e14',
            'primary-dark': '#e8590c',
            'primary-light': '#ff922b',
          },
          fontFamily: {
            'inter': ['Inter', 'sans-serif'],
          }
        }
      }
    }
  </script>
  <style>
    @media print {
      .no-print { display: none !important; }
      .print-shadow { box-shadow: none !important; }
      .print-bg { background: white !important; }
      body { background: white !important; }
      .gradient-bg { background: #fd7e14 !important; -webkit-print-color-adjust: exact; color-adjust: exact; }
    }
  </style>
</head>
<body class="font-inter bg-gray-50 min-h-screen">
  <!-- Print Actions -->
  <div class="no-print fixed top-6 right-6 flex gap-3 z-50">
    <button onclick="window.print()" 
            class="flex items-center gap-2 px-4 py-2 bg-primary hover:bg-primary-dark text-white rounded-lg font-semibold transition-all duration-300 hover:shadow-lg hover:-translate-y-0.5">
      <i class="fas fa-print"></i>
      Print Invoice
    </button>
    <a href="/user/orders/<%= order._id %>" 
       class="flex items-center gap-2 px-4 py-2 bg-gray-600 hover:bg-gray-700 text-white rounded-lg font-semibold transition-all duration-300 hover:shadow-lg hover:-translate-y-0.5">
      <i class="fas fa-arrow-left"></i>
      Back to Order
    </a>
  </div>

  <div class="max-w-4xl mx-auto p-6 lg:p-8">
    <!-- Invoice Container -->
    <div class="bg-white rounded-3xl shadow-2xl overflow-hidden print-shadow">
      
      <!-- Header Section -->
      <div class="gradient-bg bg-gradient-to-br from-primary to-primary-light text-white p-8 lg:p-12 relative overflow-hidden">
        <!-- Decorative Elements -->
        <div class="absolute top-0 right-0 w-64 h-64 bg-white opacity-5 rounded-full -translate-y-32 translate-x-32"></div>
        <div class="absolute bottom-0 left-0 w-48 h-48 bg-white opacity-5 rounded-full translate-y-24 -translate-x-24"></div>
        
        <div class="relative z-10">
          <div class="flex flex-col lg:flex-row justify-between items-start gap-8">
            <!-- Company Info -->
            <div class="flex-1">
              <div class="flex items-center gap-4 mb-6">
                <div class="w-16 h-16 bg-white bg-opacity-20 rounded-2xl flex items-center justify-center backdrop-blur-sm">
                  <i class="fas fa-bolt text-2xl text-white"></i>
                </div>
                <div>
                  <h1 class="text-4xl font-bold">ShadElectro</h1>
                  <p class="text-white text-opacity-90 text-lg">Premium Electronics Store</p>
                </div>
              </div>
              
              <div class="bg-white bg-opacity-15 backdrop-blur-sm rounded-2xl p-6 text-sm leading-relaxed">
                <div class="font-semibold text-lg mb-2">ShadElectro Technologies Pvt. Ltd.</div>
                <div class="space-y-1 text-white text-opacity-90">
                  <div class="flex items-center gap-2">
                    <i class="fas fa-map-marker-alt w-4"></i>
                    <span>123 Tech Street, Electronic City, Bangalore, Karnataka 560100</span>
                  </div>
                  <div class="flex items-center gap-2">
                    <i class="fas fa-phone w-4"></i>
                    <span>+91 80 1234 5678</span>
                  </div>
                  <div class="flex items-center gap-2">
                    <i class="fas fa-envelope w-4"></i>
                    <span>support@shadelectro.com</span>
                  </div>
                  <div class="flex items-center gap-2">
                    <i class="fas fa-file-invoice w-4"></i>
                    <span>GST: 29ABCDE1234F1Z5</span>
                  </div>
                </div>
              </div>
            </div>
            
            <!-- Invoice Info -->
            <div class="flex-1 lg:max-w-md">
              <div class="text-right mb-6">
                <h2 class="text-5xl font-bold mb-2">ITEM INVOICE</h2>
                <p class="text-white text-opacity-90 text-lg">Individual Item Receipt</p>
              </div>
              
              <div class="bg-white bg-opacity-15 backdrop-blur-sm rounded-2xl p-6">
                <div class="space-y-4">
                  <div class="flex justify-between items-center py-2 border-b border-white border-opacity-20">
                    <span class="text-white text-opacity-80">Invoice Number:</span>
                    <span class="font-bold text-lg">ITM-<%= order.orderId.substring(0, 8).toUpperCase() %>-<%= itemIndex %></span>
                  </div>
                  <div class="flex justify-between items-center py-2 border-b border-white border-opacity-20">
                    <span class="text-white text-opacity-80">Order ID:</span>
                    <span class="font-semibold">#<%= order.orderId.substring(0, 12).toUpperCase() %></span>
                  </div>
                  <div class="flex justify-between items-center py-2 border-b border-white border-opacity-20">
                    <span class="text-white text-opacity-80">Invoice Date:</span>
                    <span class="font-semibold">
                      <%= new Date(order.createdOn).toLocaleDateString('en-IN', { 
                        year: 'numeric', 
                        month: 'short', 
                        day: 'numeric' 
                      }) %>
                    </span>
                  </div>
                  <div class="flex justify-between items-center py-2">
                    <span class="text-white text-opacity-80">Item Status:</span>
                    <span class="font-bold text-green-200">
                      <i class="fas fa-check-circle mr-1"></i>
                      <%= item.status %>
                    </span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Item Highlight Section -->
      <div class="p-8 lg:p-12 bg-gradient-to-br from-orange-50 to-yellow-50">
        <div class="flex items-center gap-3 mb-8">
          <div class="w-12 h-12 bg-primary bg-opacity-10 rounded-xl flex items-center justify-center">
            <i class="fas fa-star text-primary text-xl"></i>
          </div>
          <h2 class="text-2xl font-bold text-gray-800">Featured Item</h2>
        </div>
        
        <div class="bg-white rounded-3xl p-8 shadow-xl border border-orange-200">
          <div class="flex flex-col lg:flex-row items-center gap-8">
            <!-- Item Image -->
            <div class="flex-shrink-0">
              <% if (item.product && item.product.variants && item.product.variants[item.variantIndex]) { %>
                <img src="/uploads/productImages/<%= item.product.variants[item.variantIndex].productImage[0] %>" 
                     alt="<%= item.product.productName %>" 
                     class="w-48 h-48 rounded-2xl object-cover border-4 border-primary shadow-lg">
              <% } else { %>
                <div class="w-48 h-48 bg-gray-100 rounded-2xl flex items-center justify-center border-4 border-gray-300">
                  <i class="fas fa-image text-gray-400 text-6xl"></i>
                </div>
              <% } %>
            </div>
            
            <!-- Item Details -->
            <div class="flex-1 text-center lg:text-left">
              <h3 class="text-3xl font-bold text-gray-800 mb-4">
                <%= item.product ? item.product.productName : 'Product Not Found' %>
              </h3>
              
              <% if (item.product && item.product.variants && item.product.variants[item.variantIndex]) { %>
                <div class="inline-block bg-primary bg-opacity-10 text-primary px-4 py-2 rounded-full font-semibold mb-4">
                  Variant: <%= item.product.variants[item.variantIndex].type %>
                </div>
              <% } %>
              
              <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-6">
                <div class="bg-gray-50 rounded-xl p-4 text-center">
                  <div class="text-2xl font-bold text-primary"><%= item.quantity %></div>
                  <div class="text-sm text-gray-600">Quantity</div>
                </div>
                <div class="bg-gray-50 rounded-xl p-4 text-center">
                  <div class="text-2xl font-bold text-primary">₹<%= item.price.toFixed(2) %></div>
                  <div class="text-sm text-gray-600">Unit Price</div>
                </div>
                <div class="bg-gray-50 rounded-xl p-4 text-center">
                  <div class="text-2xl font-bold text-primary">₹<%= (item.quantity * item.price).toFixed(2) %></div>
                  <div class="text-sm text-gray-600">Total</div>
                </div>
                <div class="bg-gray-50 rounded-xl p-4 text-center">
                  <% 
                    let statusClass = '';
                    let statusIcon = '';
                    switch(item.status.toLowerCase()) {
                      case 'delivered':
                        statusClass = 'text-green-600';
                        statusIcon = 'fas fa-check-circle';
                        break;
                      case 'processing':
                        statusClass = 'text-blue-600';
                        statusIcon = 'fas fa-cog';
                        break;
                      case 'shipped':
                        statusClass = 'text-purple-600';
                        statusIcon = 'fas fa-truck';
                        break;
                      case 'pending':
                        statusClass = 'text-yellow-600';
                        statusIcon = 'fas fa-clock';
                        break;
                      case 'cancelled':
                        statusClass = 'text-red-600';
                        statusIcon = 'fas fa-times-circle';
                        break;
                      default:
                        statusClass = 'text-gray-600';
                        statusIcon = 'fas fa-info-circle';
                    }
                  %>
                  <div class="text-2xl font-bold <%= statusClass %>">
                    <i class="<%= statusIcon %>"></i>
                  </div>
                  <div class="text-sm text-gray-600"><%= item.status %></div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Billing Information -->
      <div class="p-8 lg:p-12 bg-gray-50">
        <div class="grid md:grid-cols-2 gap-8">
          <!-- Bill To -->
          <div class="bg-white rounded-2xl p-6 shadow-lg border border-gray-100">
            <div class="flex items-center gap-3 mb-6">
              <div class="w-10 h-10 bg-primary bg-opacity-10 rounded-xl flex items-center justify-center">
                <i class="fas fa-user text-primary"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-800">Bill To</h3>
            </div>
            <div class="space-y-2 text-gray-600 leading-relaxed">
              <div class="font-bold text-gray-800 text-lg"><%= order.userId.name %></div>
              <div><%= order.shippingAddress.address %></div>
              <% if (order.shippingAddress.landMark) { %>
                <div><%= order.shippingAddress.landMark %></div>
              <% } %>
              <div><%= order.shippingAddress.city %>, <%= order.shippingAddress.state %></div>
              <div><%= order.shippingAddress.pinCode %>, <%= order.shippingAddress.country %></div>
              <% if (order.shippingAddress.phone) { %>
                <div class="flex items-center gap-2 mt-3">
                  <i class="fas fa-phone text-primary w-4"></i>
                  <span><%= order.shippingAddress.phone %></span>
                </div>
              <% } %>
              <div class="flex items-center gap-2">
                <i class="fas fa-envelope text-primary w-4"></i>
                <span><%= order.userId.email %></span>
              </div>
            </div>
          </div>
          
          <!-- Order Context -->
          <div class="bg-white rounded-2xl p-6 shadow-lg border border-gray-100">
            <div class="flex items-center gap-3 mb-6">
              <div class="w-10 h-10 bg-blue-100 rounded-xl flex items-center justify-center">
                <i class="fas fa-receipt text-blue-600"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-800">Order Context</h3>
            </div>
            <div class="space-y-4">
              <div class="flex justify-between items-center py-2 border-b border-gray-100">
                <span class="text-gray-600">Full Order ID:</span>
                <span class="font-semibold text-gray-800">#<%= order.orderId.substring(0, 12).toUpperCase() %></span>
              </div>
              <div class="flex justify-between items-center py-2 border-b border-gray-100">
                <span class="text-gray-600">Order Date:</span>
                <span class="font-semibold text-gray-800">
                  <%= new Date(order.createdOn).toLocaleDateString('en-IN', { 
                    year: 'numeric', 
                    month: 'long', 
                    day: 'numeric' 
                  }) %>
                </span>
              </div>
              <div class="flex justify-between items-center py-2 border-b border-gray-100">
                <span class="text-gray-600">Total Items in Order:</span>
                <span class="font-semibold text-gray-800"><%= order.orderedItems.length %></span>
              </div>
              <div class="flex justify-between items-center py-2 border-b border-gray-100">
                <span class="text-gray-600">Payment Method:</span>
                <span class="font-semibold text-gray-800"><%= order.paymentMethod %></span>
              </div>
              <div class="flex justify-between items-center py-2">
                <span class="text-gray-600">Payment Status:</span>
                <span class="inline-flex items-center gap-1 px-2 py-1 bg-green-100 text-green-800 rounded-full text-xs font-semibold">
                  <i class="fas fa-check-circle"></i>
                  <%= order.paymentStatus %>
                </span>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Item Summary -->
      <div class="p-8 lg:p-12">
        <div class="max-w-md mx-auto">
          <div class="bg-white rounded-2xl p-6 shadow-lg border border-gray-100">
            <div class="flex items-center gap-3 mb-6">
              <div class="w-10 h-10 bg-primary bg-opacity-10 rounded-xl flex items-center justify-center">
                <i class="fas fa-calculator text-primary"></i>
              </div>
              <h3 class="text-xl font-bold text-gray-800">Item Summary</h3>
            </div>
            
            <div class="space-y-4">
              <div class="flex justify-between items-center py-2">
                <span class="text-gray-600">Item Price</span>
                <span class="font-semibold text-gray-800">₹<%= item.price.toFixed(2) %></span>
              </div>
              
              <div class="flex justify-between items-center py-2">
                <span class="text-gray-600">Quantity</span>
                <span class="font-semibold text-gray-800"><%= item.quantity %></span>
              </div>
              
              <div class="flex justify-between items-center py-2">
                <span class="text-gray-600">Subtotal</span>
                <span class="font-semibold text-gray-800">₹<%= (item.quantity * item.price).toFixed(2) %></span>
              </div>
              
              <div class="flex justify-between items-center py-2">
                <span class="text-gray-600">Tax (GST 18%)</span>
                <span class="font-semibold text-gray-800">₹<%= ((item.quantity * item.price) * 0.18 / 1.18).toFixed(2) %></span>
              </div>
              
              <div class="border-t-2 border-primary pt-4">
                <div class="flex justify-between items-center">
                  <span class="text-xl font-bold text-gray-800">Item Total</span>
                  <span class="text-2xl font-bold text-primary">₹<%= (item.quantity * item.price).toFixed(2) %></span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Terms & Footer -->
      <div class="p-8 lg:p-12 bg-gradient-to-br from-gray-800 to-gray-900 text-white">
        <div class="grid md:grid-cols-2 gap-8">
          <!-- Terms & Conditions -->
          <div>
            <h3 class="text-xl font-bold mb-4 flex items-center gap-2">
              <i class="fas fa-file-contract"></i>
              Terms & Conditions
            </h3>
            <div class="space-y-2 text-gray-300 text-sm leading-relaxed">
              <p>• This is an individual item invoice from your complete order.</p>
              <p>• This invoice is computer generated and does not require a physical signature.</p>
              <p>• For any queries regarding this item, please contact our customer support.</p>
              <p>• Return policy: This item can be returned within 30 days of delivery in original condition.</p>
              <p>• Warranty terms apply as per product specifications.</p>
            </div>
          </div>
          
          <!-- Company Signature -->
          <div class="text-center md:text-right">
            <div class="mb-6">
              <div class="text-2xl font-bold text-primary mb-2">Thank you for choosing this product!</div>
              <p class="text-gray-300">We hope you enjoy your purchase from ShadElectro.</p>
            </div>
            
            <div class="bg-white bg-opacity-10 backdrop-blur-sm rounded-xl p-4">
              <p class="text-sm text-gray-300 mb-2">For support and inquiries:</p>
              <div class="space-y-1 text-sm">
                <div class="flex items-center justify-center md:justify-end gap-2">
                  <i class="fas fa-globe"></i>
                  <span>www.shadelectro.com</span>
                </div>
                <div class="flex items-center justify-center md:justify-end gap-2">
                  <i class="fas fa-phone"></i>
                  <span>+91 80 1234 5678</span>
                </div>
                <div class="flex items-center justify-center md:justify-end gap-2">
                  <i class="fas fa-envelope"></i>
                  <span>support@shadelectro.com</span>
                </div>
              </div>
            </div>
          </div>
        </div>
        
        <div class="text-center mt-8 pt-8 border-t border-gray-700">
          <p class="text-gray-400 text-sm">
            Item invoice generated on <%= new Date().toLocaleDateString('en-IN', { 
              year: 'numeric', 
              month: 'long', 
              day: 'numeric',
              hour: '2-digit',
              minute: '2-digit'
            }) %>
          </p>
        </div>
      </div>
    </div>
  </div>

  <script>
    // Auto-print if print parameter is present
    const urlParams = new URLSearchParams(window.location.search);
    if (urlParams.get('print') === 'true') {
      window.onload = function() {
        setTimeout(() => {
          window.print();
        }, 500);
      };
    }
  </script>
</body>
</html>
